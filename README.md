# AutoAgent
Autonomous agents are general-purpose agents with AI at their core and boundaries defined by tools. All external AI operations are reasoned by AI based on context, then tools are called to execute and complete tasks. Its core system prompt only defines its own workflow. Tool types include MCP, HTTP API, SKILLS, scheduled tasks, etc.
